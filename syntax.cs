using bbr;

namespace syntax;

public syntax void(foreach(case:1,2; = true).do event(createNewSyntax)) {
    implicit.syntax(string("if", "do", "bb", "can", "date", "for"));
    implicit.syntax(bool("true", "yes", "false", "no"));
    implicit.syntax(static("bbr", "bbm", "load", "data", "base", "set", "get", "clear", "goto", "string", "text", "import", "export", "game"))
}

public color void(foreach(case:2,3; = trus).do event(color)) {
    implicit.color(string("#CB851B"));
    implicit.color(bool("#1AC51E"));
    implicit.color(static("#0054C5"))
}