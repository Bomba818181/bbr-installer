using bbr;

namespace bbr

void (implicit databse) public class (do 6) {
    double.event(do string("local","model".abstract = true,abstract(if model = true do {
        as bbr case 1; do event(string(bbr.setLocal = true;0,this.foreach;)case 2 do(event"getStrict";)public+static(short;)),
        do(event.lock);
    })));
},